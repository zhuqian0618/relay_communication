# 基于编码演化的“初始纯CE + NN热启动CE”初学者指南

## 1. 这套系统解决什么问题

实验开始时，系统不知道合适编码，因此先用均匀概率运行一次普通CE。之后保持最优编码并持续读取频谱仪。最近5次读数的中位数相对当前功率锚点下降至少2 dB时，才启动下一次优化。

神经网络不接收角度、方向或频谱仪功率。功率只负责判断“何时重新优化”；网络只根据控制器已经保存的编码变化，预测下一次CE的初始概率分布。

```text
实验开始
  ↓
均匀概率纯CE → 当前最佳编码 → 测5次建立局部功率锚点
                                  ↓
                           持续读取频谱仪
                                  ↓
                  最近5次中位数是否下降至少2 dB？
                         否 ↙                 ↘ 是
                      继续监测          编码历史送入MLP
                                                ↓
                                      预测30×4状态概率
                                                ↓
                                      第一代即热启动CE
                                                ↓
                                  新最佳编码并重建功率锚点
```

## 2. 241维输入到底是什么

两块RIS共有32个编码变量，其中联合变量0和16是固定参考列，恒为状态0。网络只处理其余30个自由变量。

```text
最近一次最佳编码one-hot                  30×4 = 120维
最近一次与前一次最佳编码的模4变化one-hot  30×4 = 120维
历史是否有效                                      1维
总计                                            241维
```

模4变化 `(latest - previous) % 4` 的四类是：0保持、1增加一级、2增加两级、3减少一级。第一次触发时还没有前一次变化，变化全部置0，历史有效标志置0。

这种输入并不能直接告诉网络无人机顺时针还是逆时针。它只利用“上一次最优编码怎样变化”作为间接线索。因此训练准确率不一定很高，最终评价必须以热启动CE是否在不损失功率质量的前提下减少迭代为准。

## 3. MLP网络结构

```text
[batch, 241]
      ↓ Linear(241,128) + ReLU
[batch, 128]
      ↓ Linear(128,64) + ReLU
[batch, 64]
      ↓ Linear(64,120)
[batch, 30,4]
```

这是全连接神经网络MLP，不是CNN。这里逐层降维，是因为241维编码历史先被压缩为128维摘要，再压缩为64维，最后输出30个变量各4种状态的logits。

## 4. 如何生成训练数据

仿真内部可以用角度计算信道，但角度不会进入网络特征：

1. 每条轨迹起点运行均匀概率纯CE；
2. 保持该编码，模拟UAV2逐步移动；
3. 频谱仪相对局部锚点下降2 dB时产生一个训练事件；
4. 保存触发前的两次最佳编码历史；
5. 在该位置重新运行共享的均匀概率纯CE；
6. 把纯CE的历史最佳编码和终止概率作为监督标签；
7. 更新最佳编码和局部锚点，继续移动。

训练损失为：

```text
0.70 × 最佳编码加权交叉熵 + 0.30 × 终止概率KL散度
```

相对当前编码发生变化的列权重为3，避免模型只学习“复制旧编码”。默认用6组独立随机轨迹生成更多事件，降低单次随机CE标签的影响。

## 5. 冷启动与热启动如何保证公平

冷、热启动共同调用 `relay_sim/CE_Optimizer.py` 中唯一的 `run_ce()`。候选数、最大代数、精英比例、平滑、最小概率、导频数、固定参考列和停止条件完全相同，唯一差别是第一代之前传入的初始概率。

第一次触发：

```text
p0 = 0.30×NN概率 + 0.40×上一编码 + 0.30×均匀概率
```

第二次及以后：

```text
p0 = 0.60×NN概率 + 0.30×上一编码 + 0.10×均匀概率
```

统一停止条件只有：平均最大状态概率达到0.90，或者达到25代。最终结果始终来自实际评估过的历史候选。

## 6. 文件阅读和运行顺序

1. `relay_sim/CE_Optimizer.py`：共享CE循环；
2. `model.py`：241维特征和MLP；
3. `train.py`：事件生成、训练、验证和曲线；
4. `ce_demo.py`：完整闭环与冷/热公平对照；
5. `Main_dual_ris_channel_simulation.py`：原仿真主程序，现在也调用共享CE。

在 `relay_communication` 环境中运行：

```powershell
D:\anaconda3\envs\relay_communication\python.exe train.py
D:\anaconda3\envs\relay_communication\python.exe ce_demo.py
```

训练产生 `code_evolution_net.pth`、`code_evolution_training.png` 和 `code_evolution_heatmap.png`；演示产生 `cold_warm_ce_comparison.png`。

## 7. 怎样判断模型真的有用

先检查功率质量，再比较速度：

- 热启动相对冷启动的功率差中位数不低于-0.5 dB；
- 至少95%的事件不比冷启动差1 dB以上；
- 热启动达到最大代数的比例不高于冷启动。

只有这些质量条件全部通过，才报告迭代代数和频谱仪读取数的下降。`ce_demo.py` 会打印 `Quality gate: PASS/FAIL`；若失败，会明确提示不能宣称加速。

## 8. 接入真实频谱仪时替换哪里

仿真中的 `make_simulated_measurement(angle_deg)` 只是产生 `measure(code) -> power_dBm`。实机时用频谱仪和RIS控制器实现同样接口：先下发编码，等待稳定，再读取功率。然后用 `make_scalar_measurement_evaluator()` 接入共享CE；它会按 `pilot_symbols_L=4` 对每个候选读取4次，在瓦特域平均并计算SNR。角度仅属于仿真器内部，不能进入实机网络调用。
